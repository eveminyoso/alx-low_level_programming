#include "main.h"
/**
 * _puts - print a string followed by new line
 * @str: string to print
 * @s: character to be described
 */
void _puts(char *s)
{
	const char* str =  Hello world
	while (*str != '\0')
	{
		putchar(*str++);
	}
		_putchar('\n');
}
